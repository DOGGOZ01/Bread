module.exports = {
    name: 'start',
    args: false,
    admin: true,
    loaded: true,
    aliases: null,

    run: async (message, args, client, Discord) => {

        const embed = new Discord.MessageEmbed()
            .setTitle()
            .setColor()
            .setDescription()
            .addFields()
            .setFooter()

    }
}